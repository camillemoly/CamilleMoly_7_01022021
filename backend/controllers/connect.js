const initModels = require('../models/init-models');
const models = initModels(sequelize);

// Signup
exports.signup = () => {
    // const newUser = users.create({ email:'camille@test.com', password:'123', first_name:'Camille', last_name:'Moly' });
    // console.log('User créé !!!')
};

// Login
exports.login = () => {

};